import { Component } from '@angular/core';

@Component({
  selector: 'app-projects',
  template: ` <section class="projects-section"></section> `,
  standalone: true,
})
export class ProjectsComponent {}
